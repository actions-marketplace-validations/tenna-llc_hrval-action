apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: azu-server
  namespace: ingestion
  # annotations:
  #   fluxcd.io/automated: 'true'
spec:
  helmVersion: v3
  maxHistory: 20
  releaseName: azu-server
  chart:
    repository: http://helm.cloud.tenna.com
    name: rmq-processor
    version: 1.3.3
  values:
    name: azu-server
    environment: qa1
    service:
      image:
        repository: docker.pkg.github.com/tenna-llc/hapi-server-ingestion/hapi-server-azu
        tag: v4.4.0-18
      env:
      - name: NODE_ENV
        value: qa1